{{- if eq .osid "arch" -}}
/* GENERATED BY CHEZMOI - Integration for Omarchy Theme */
@import "../omarchy/current/theme/waybar.css";

* {
  background-color: @background;
  color: @foreground;
  border: none;
  border-radius: 0;
  min-height: 0;
  font-family: 'CaskaydiaMono Nerd Font';
  font-size: 12px;
}

/* ... existing styles ... */

{{- if eq .role "laptop" }}
#battery {
  min-width: 12px;
  margin: 0 7.5px;
}
{{- end }}

/* Keep the rest of your layout logic */
.modules-left { margin-left: 8px; }
.modules-right { margin-right: 8px; }
/* ... (remaining existing CSS) ... */

{{- end }}
